# =============================================================
# CD - Déploiement AUTOMATIQUE
# Repo : donaldte/microservice-cicd
# Déploie automatiquement sur ton serveur Docker Swarm
# =============================================================

name: CD - Deploy Production

on:
  workflow_run:
    workflows: ["CI - Main Build & Push"]
    types:
      - completed
    branches: [ main ]

jobs:
  deploy:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest

    steps:
      - name: SSH → Clone/Pull + Déploiement auto
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: 22
          timeout: 90s

          script: |
            set -e

            echo "Connexion SSH réussie sur $(hostname)"

            cd /opt

            # 1. Si dossier existe sans .git → supprimer
            if [ -d "cloudtaskhub" ] && [ ! -d "cloudtaskhub/.git" ]; then
              echo "⚠️ Dossier cloudtaskhub existe mais n'est pas un dépôt Git → suppression..."
              rm -rf cloudtaskhub
            fi

            # 2. Clone si dossier absent
            if [ ! -d "cloudtaskhub" ]; then
              echo "📥 Premier déploiement → git clone..."
              git clone https://github.com/donaldte/microservice-cicd.git cloudtaskhub
            fi

            cd cloudtaskhub

            echo "🔄 Mise à jour du code..."
            git fetch --all
            git checkout main
            git pull origin main

            # EXPORT VARIABLES (critical fix)
            echo "🌍 Exporting environment variables for Swarm..."
            export IMAGE_TAG="latest"
            export DOCKERHUB_USERNAME="${{ secrets.DOCKERHUB_USERNAME }}"

            echo "Using DOCKERHUB_USERNAME=$DOCKERHUB_USERNAME"
            echo "Using IMAGE_TAG=$IMAGE_TAG"

            echo "🚀 Déploiement Docker Swarm..."
            docker stack deploy \
              -c docker-compose.prod.yml \
              cloudtaskhub \
              --with-registry-auth \
              --resolve-image changed \
              --prune

            echo "✅ DÉPLOIEMENT TERMINÉ AVEC SUCCÈS !"
            docker stack ps cloudtaskhub --no-trunc
            docker service ls

  # =============================================================
  # 3. Notification Slack en cas d’échec
  # =============================================================
  slack-failure:
    needs: deploy
    if: failure()                             # seulement si les tests ont planté
    uses: ./.github/workflows/slack-notify.yml
    with:
      status: failure
      title: "Le Deploy du PR ❌ a échoué"
      message: |
        Auteur : ${{ github.actor }}
    secrets: inherit

  # =============================================================
  # 4. Notification Slack si tout est bon (optionnel mais pro)
  # =============================================================
  slack-success:
    needs:  deploy
    if: success()
    uses: ./.github/workflows/slack-notify.yml
    with:
      status: success
      title: "Le Build du PR ✅  a réussi"
      message: |
        Le déploiement a réussi !
        Auteur : ${{ github.actor }}
    secrets: inherit  

